#!/bin/sh
# Gradle wrapper stub — will be replaced by Android Studio on first sync
echo "Please open this project in Android Studio to generate the full Gradle wrapper."
echo "Or run: gradle wrapper --gradle-version 8.11.1"
exit 1
